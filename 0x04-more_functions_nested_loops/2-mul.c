#include "main.h"

/**
 * mul - functions that multipies two integers
 * @a: first num to be multiplied
 * @b: second num to be multiplied
 * Return: The result of the multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
