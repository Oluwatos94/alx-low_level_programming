#include "main.h"

/***
 * multiple - functions that multipies two integers
 * @a: first num to be multiplied
 * @b: second num to be multiplied
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
